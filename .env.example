# Oakland 311 API Token (optional)
# Leave empty if you don't have an API token - the API works without authentication
OAK311_API_TOKEN=

# SQLite database path (optional)
# Defaults to ./data/illegal-dumping.db
DATABASE_PATH=
